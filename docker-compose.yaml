version: "3.8"

services:
  db:
    image: postgres:15.1
    hostname: db
    container_name: palmpay_advisor_db
    restart: always
    ports:
      - 6005:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=palmpay
      - POSTGRES_DB=devdb
      - POSTGRES_HOST=localhost
      - POSTGRES_PORT=5432
    cap_add:
      - SYS_NICE
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres", "-d", "devdb"]
      interval: 10s
      timeout: 5s
      retries: 5
